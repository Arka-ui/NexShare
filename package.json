{
  "name": "nexshare",
  "version": "0.0.0",
  "private": true,
  "description": "NexShare — security unit tests (Node built-in test runner; no runtime or dev dependencies).",
  "scripts": {
    "test": "node --test"
  }
}
